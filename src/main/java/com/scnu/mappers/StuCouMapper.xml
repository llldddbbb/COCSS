<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.scnu.dao.StuCouMapper">
    <resultMap id="BaseResultMap" type="com.scnu.entity.StuCou">
        <id column="id" property="id" jdbcType="INTEGER"/>
        <id column="courseId" property="courseId" jdbcType="INTEGER"/>
        <result column="state" property="state" jdbcType="INTEGER"/>
        <result column="createTime" property="createTime" jdbcType="TIMESTAMP"/>
        <result column="stuId" property="stuId" jdbcType="INTEGER"/>
        <association column="courseId" property="course" select="com.scnu.dao.CourseMapper.getCourseById"></association>
    </resultMap>

    <insert id="insertStuCou">
        insert into stucou(courseId,state,createTime,stuId)
        values(#{courseId},0,now(),#{stuId})
    </insert>

    <select id="getByStuIdWithCourse" resultMap="BaseResultMap">
        select * from stucou
        where stuId=#{stuId}
        and courseId=#{courseId}
    </select>

</mapper>